// @flow

export type SiteInformation = {
  artifact_id: string,
  filesDirectory: string,
  latestPublishedDirectory: string,
  inEditingMode: boolean,
  isMaDesignerDisabled: boolean,
  siteConfiguration: Object,
  siteDirectory: string,
}
